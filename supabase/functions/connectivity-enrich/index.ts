// Connectivity V2 implementation lives in the sibling source file so it can be
// reviewed separately while preserving the existing deployed function slug.
import "../connectivity-enrich-v2/index.ts";
